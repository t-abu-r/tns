/**
 * TNS — Cloudinary Configuration
 * Add your Cloudinary cloud name and unsigned upload preset below.
 * These values come from your Cloudinary dashboard.
 *
 * For Vercel deployment, these are baked into the static build.
 * Upload presets should be set to "Unsigned" in Cloudinary settings.
 */
window.TNS_CLOUDINARY = {
  cloudName: 'YOUR_CLOUD_NAME',
  uploadPreset: 'YOUR_UPLOAD_PRESET'
};
